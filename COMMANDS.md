# 🦉 ARGUS — Справочник Команд

---

## 🤖 Команды Telegram-бота

| Команда | Описание |
|:---|:---|
| `/start` | → Приветствие, знакомство с ботом |
| `/help` | → Краткая справка по использованию |
| `/ask <вопрос>` | → Поиск ответа в базе знаний (книги). Ответ приходит через 30-60 сек |
| `/stats` | → Показать количество книг и чанков в базе |

**Пример:** `/ask Что такое риск-менеджмент?`

---

## 🔘 Кнопки в Telegram

| Кнопка | Где появляется | Что делает |
|:---|:---|:---|
| `✅ Скачать` | Карточка кандидата (книга) | → Запускает скачивание PDF в репозиторий |
| `❌ Отклонить` | Карточка кандидата (книга) | → Удаляет кандидата из очереди |
| `✅ Опубликовать` | Черновик поста | → Публикует пост в TG-канал и VK |
| `🔄 Перегенерировать` | Черновик поста | → Создаёт новый вариант поста |
| `❌ Удалить` | Черновик поста | → Удаляет черновик без публикации |

---

## ⚙️ GitHub Actions (ручной запуск)

Запускаются через вкладку **Actions** → выбери workflow → **Run workflow**.

| Workflow | Что делает |
|:---|:---|
| `ARGUS Train Model` | → Полный цикл: ingest → train → build_index → очистка книг |
| `ARGUS Search` | → Ручной поисковый запрос (тест) |
| `ARGUS Guardian` | → Тест качества поиска / снимок / откат (выбери mode) |
| `ARGUS Benchmark` | → Проверка точности поиска на эталонных вопросах |
| `ARGUS Explorer` | → Поиск новых книг по теме (можно указать тему вручную) |
| `ARGUS Actor` | → Выполнение накопленных предложений (скачивание, пересборка) |
| `ARGUS Analyzer` | → Анализ логов + формирование предложений |
| `ARGUS Proposer` | → Формирование плана действий из анализа |
| `ARGUS Reporter` | → Отправить еженедельный отчёт в Telegram |
| `ARGUS Collect Data` | → Собрать свежие цены BTC/ETH с бирж |
| `ARGUS Content Post` | → Сгенерировать черновик поста |
| `Check Token` | → Проверить, что токен GH_PAT работает |

---

## ⏰ Автоматические задачи (Cron)

| Workflow | Расписание | Что делает |
|:---|:---|:---|
| `ARGUS Brain` | Каждые 30 мин | → Думает: анализирует → предлагает → действует |
| `ARGUS Collect Data` | Каждый час | → Собирает цены и Fear & Greed |
| `ARGUS Benchmark` | Воскресенье 05:00 UTC | → Проверяет качество поиска |
| `ARGUS Reporter` | Понедельник 09:00 UTC | → Шлёт недельный отчёт |

---

## 🔄 Типичные сценарии

**Добавить новые книги:**
1. Загрузи PDF в папку `books/` через GitHub
2. Напиши боту `/train` или запусти `ARGUS Train Model` в Actions
3. Подожди отчёт в Telegram 🎓

**Задать вопрос:**
1. Напиши `/ask Твой вопрос`
2. Подожди 30-60 секунд
3. Получи ответ из книг 📚

**Проверить здоровье системы:**
1. Запусти `ARGUS Guardian` (mode: test)
2. Посмотри точность в Telegram 🎯

**Найти новые книги автоматически:**
1. Запусти `ARGUS Explorer` (можно указать тему)
2. Бот пришлёт карточки кандидатов
3. Нажми `✅ Скачать` или `❌ Отклонить`
